{
  "scripts": {
    "sv": "json-server --watch db.json --port 3001",
    "files": "node index.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "json-server": "^1.0.0-beta.3",
    "multer": "^2.0.2"
  }
}
